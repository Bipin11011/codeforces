#include <stdio.h>

int main() {
    int a, b;
    scanf("%d %d", &a, &b);

    int different = (a < b) ? a : b;
    int same = (a > b) ? (a - b) / 2 : (b - a) / 2;

    printf("%d %d\n", different, same);

    return 0;
}